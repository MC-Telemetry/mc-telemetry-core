# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
# This workflow will build a Java project with Gradle and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-gradle

name: Gradle build and check on main

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-common:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@af1da67850ed9a4cedd57bfd976089dd991e2582 # v4.0.0

    - name: Build
      run: ./gradlew :common:build

  
  build-mod:
    needs:
      - build-common
    runs-on: ubuntu-latest
    permissions:
      contents: read
    strategy:
      matrix:
        project: ["neoforge", "fabric"]

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@af1da67850ed9a4cedd57bfd976089dd991e2582 # v4.0.0

    - name: Check
      id: check
      run: ./gradlew :$project:check
    - name: Upload GameTest logs
      uses: actions/upload-artifact@v4
      if: ${{ success() || (failure() && steps.check.outcome == 'failure') }}
      with:
        name: ${{project}}-gametest-logs
        path: |
          $project/gameTestRun/logs/latest.log
          $project/gameTestRun/logs/debug.log
        if-no-files-found: error
    - name: Upload GameTest world
      uses: actions/upload-artifact@v4
      if: ${{ failure() && steps.check.outcome == 'failure' }}
      with:
        name: ${{project}}-gametest-world
        path: |
          $project/gameTestRun/world/
        retention-days: 3
        if-no-files-found: error

    - name: Build
      run: ./gradlew :${{project}}:build -x :${{project}}:check
    - name: Upload Build result
      uses: actions/upload-artifact@v4
      with:
        name: ${{project}}-gametest-world
        path: |
          $project/build/libs/
        retention-days: 1


  dependency-submission:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'

    # Generates and submits a dependency graph, enabling Dependabot Alerts for all project dependencies.
    # See: https://github.com/gradle/actions/blob/main/dependency-submission/README.md
    - name: Generate and submit dependency graph
      uses: gradle/actions/dependency-submission@af1da67850ed9a4cedd57bfd976089dd991e2582 # v4.0.0
