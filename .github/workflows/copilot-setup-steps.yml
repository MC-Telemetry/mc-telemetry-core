name: Copilot Setup Steps

on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/copilot-setup-steps.yml
  pull_request:
    paths:
      - .github/workflows/copilot-setup-steps.yml

jobs:
  copilot-setup-steps:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4
      
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
      
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@4d9f0ba0025fe599b4ebab900eb7f3a1d93ef4c2 # v5.0.0
      
      - name: Download dependencies
        run: |
          ./gradlew dependencies --no-daemon || true
          ./gradlew :common:dependencies --no-daemon || true
          ./gradlew :fabric:dependencies --no-daemon || true
          ./gradlew :neoforge:dependencies --no-daemon || true
      
      - name: Download OpenTelemetry agent
        run: ./gradlew downloadOTelAgent verifyOTelAgent --no-daemon || true
      
      - name: Accept EULA
        run: |
          mkdir -p neoforge/gameTestRun
          mkdir -p fabric/gameTestRun
          cat <<EOF > neoforge/gameTestRun/eula.txt
          #By changing the setting below to TRUE you are indicating your agreement to our EULA (https://aka.ms/MinecraftEULA).
          #Fri Oct 31 15:37:45 CET 2025
          eula=true
          EOF
          cat <<EOF > neoforge/gameTestRun/eula.txt
          #By changing the setting below to TRUE you are indicating your agreement to our EULA (https://aka.ms/MinecraftEULA).
          #Fri Oct 31 15:37:45 CET 2025
          eula=true
          EOF

